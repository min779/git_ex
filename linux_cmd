$rm -r <dir_name>  // 하위 디렉토리까지 삭제

$mkdir // produce dir

$cp <file or dir name> <file2 of dir2 name>  // 복사

$cp -a // 또는 --archive, 속성도 같이 복사

$cp -i // 또는 --interactive, 기존 파일을 확인하고 덮어쓴다

$cp -r // 또는 --recursive, 디렉토리 속 내용도 함께 복사

$cp -u // 또는 --update, 디렉토리 복사할 때, 두 디렉토리를 비교하여 다른 부분만 복사할

$cp -v // 또는 --verbose, 완료 메시지 풀력

$clear // 화면 정리


// 파일 이동과 이름 변경

// file 을 file1으로 이동하거나 이름을 바꾼다

$mv <file or dir name> <file_1 or dir_1 name>

$mv -i // --interactive, 변경 또는 이동 여부를 묻는다. 

$mv -u // --update, 상이한 부분만 이동

$mv -v // --verbose, 이동 완료후 메시지를 보여준다.

// 파일 삭제
$rm -i // --interactive

$rm -v // --verbose

$rm -f // --force, 존재하지 않는 파일은 확인 없이 무시

$rm -r // --recursive, 하위 dir 도 함께 삭제, dir 을 완전히 삭제
